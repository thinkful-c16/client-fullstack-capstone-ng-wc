import {  } from './actions';

const initialState = {
  view: 'home',
  activeWordCloud: {},
  font: '',
  fontColor: ''
};

export const reducer = (state =initialState, action) => {
  return state;
};